# 监听IP
HOST: 0.0.0.0
# 监听端口
PORT: 18000

# API请求token
API_TOKEN: 
  # 是否启用
  enable: true
  # token值
  token: 1145141919810

# Cookie自动健康检查
COOKIE_CHECK:
  # 是否启用
  enable: true
  # 检查间隔(秒)
  check_intlval: 600

# Cookie自动刷新
COOKIE_REFRESH:
  # 是否启用
  enable: true
  # 刷新间隔(天)
  refresh_intlval: 30

PUSH:
  GOTIFY:
    # 是否启用
    ## 默认false
    enable: true
    url: "http://114.51.41.91:9810"
    token: "0721000000"
  EMAIL:
    # 是否启用
    enable: true